source 'https://rubygems.org'

gem 'rails', '3.2.12'


# these gems is needed for MS SQL connection
#gem 'tiny_tds'
#gem 'activerecord-sqlserver-adapter', :require => false

# this active billing gems
gem 'CIA', :require => "cia", :git => "git@github.com:activebilling/CIA.git", :branch => "pubsub"
gem 'slim-rails'

#temporary database, before use the real one:
gem 'sqlite3'

# Gems used only for assets
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'
